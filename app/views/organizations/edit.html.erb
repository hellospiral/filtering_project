<h1>Editing Organization</h1>

<%= render 'form', organization: @organization %>

<br>

<%= link_to 'Back to organization', organization_path(@organization) %> |
<%= link_to 'View all Organizations', organizations_path(@stack) %>
